{
  "name": "merkle-proof-token",
  "version": "0.1.0",
  "description": "Merkle Proof Token (MPT) — UTXO-based token for BSV with SPV-verifiable provenance",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "compile": "npx scrypt-cli compile",
    "test": "jest",
    "pretest": "npm run build"
  },
  "license": "MIT",
  "dependencies": {
    "@bsv/sdk": "^1.0.0",
    "scrypt-ts": "^1.3.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "esbuild": "^0.27.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
